const s=io();const $=id=>document.getElementById(id);let me=null,people=[],groups=[],selected={type:null,id:null,name:null},authMode='create',typingTimer=null;const SESSION='fivechat_v13_session';
function sessionSave(){localStorage.setItem(SESSION,JSON.stringify({name:me?.name||''}))}function sessionLoad(){try{return JSON.parse(localStorage.getItem(SESSION)||'null')}catch{return null}}function showApp(){ $('auth').classList.add('hidden');$('app').classList.remove('hidden');$('meName').textContent=me.name;$('meAvatar').textContent=me.name[0].toUpperCase() }
function auth(){const name=$('authName').value.trim(),code=$('authCode').value.trim();$('authError').textContent='';if(!name||!code){$('authError').textContent='Enter your name and private code.';return}s.emit(authMode==='create'?'createAccount':'login',{name,accessCode:code})}
$('authBtn').onclick=auth;['authName','authCode'].forEach(id=>$(id).onkeydown=e=>{if(e.key==='Enter')auth()});$('switchAuth').onclick=()=>{authMode=authMode==='create'?'login':'create';$('authTitle').textContent=authMode==='create'?'Create your account':'Log in to FiveChat';$('authText').textContent=authMode==='create'?'Create your name once, then use FiveChat from any device connected to the same server.':'Enter your registered name and the private access code.';$('authBtn').textContent=authMode==='create'?'Create account':'Log in';$('switchAuth').textContent=authMode==='create'?'Already have an account? Log in':'Need an account? Create one';$('authError').textContent=''};
s.on('accountCreated',d=>{authMode='login';$('authTitle').textContent='Account created';$('authText').textContent='Now log in with the same name and private access code.';$('authBtn').textContent='Log in';$('switchAuth').textContent='Need a different account? Create one';$('authName').value=d.name;$('authCode').value='';$('authError').textContent='Account created successfully.'});s.on('authError',m=>$('authError').textContent=m);
s.on('loggedIn',d=>{me=d.user;people=d.people;groups=d.groups;sessionSave();showApp();renderPeople();renderGroups();});
function renderPeople(){const q=$('search').value.toLowerCase();const arr=people.filter(p=>p.name!==me.name&&p.name.toLowerCase().includes(q));$('onlinePeople').innerHTML='';$('offlinePeople').innerHTML='';arr.sort((a,b)=>Number(b.online)-Number(a.online)||a.name.localeCompare(b.name)).forEach(p=>{const li=document.createElement('li');li.className='person '+(selected.type==='direct'&&selected.name===p.name?'selected':'');li.onclick=()=>openDirect(p);li.innerHTML=`<span class="avatar">${p.name[0].toUpperCase()}</span><span class="personText"><b>${escapeHtml(p.name)}</b><small>${p.online?'Online':'Last seen '+timeSince(p.lastSeen)}</small></span><i class="dot ${p.online?'on':'off'}"></i>`;(p.online?$('onlinePeople'):$('offlinePeople')).append(li)});$('onlineCount').textContent=arr.filter(x=>x.online).length}
function renderGroups(){$('groupList').innerHTML='';groups.forEach(g=>{const li=document.createElement('li');li.className='person '+(selected.type==='group'&&selected.id===g.id?'selected':'');li.onclick=()=>openGroup(g);li.innerHTML=`<span class="avatar groupAvatar">👥</span><span class="personText"><b>${escapeHtml(g.name)}</b><small>${g.members.length} members</small></span>`;$('groupList').append(li)})}
function openDirect(p){selected={type:'direct',name:p.name};$('chatName').textContent=p.name;$('chatAvatar').textContent=p.name[0].toUpperCase();$('chatStatus').textContent=p.online?'● Online':'Offline';$('message').disabled=false;$('send').disabled=false;renderPeople();$('messages').innerHTML='<div class="loading">Loading…</div>';s.emit('openDirect',{with:p.name});if(innerWidth<760)$('peoplePanel').classList.add('closed')}
function openGroup(g){selected={type:'group',id:g.id,name:g.name};$('chatName').textContent=g.name;$('chatAvatar').textContent='👥';$('chatStatus').textContent=g.members.length+' members';$('message').disabled=false;$('send').disabled=false;renderGroups();$('messages').innerHTML='<div class="loading">Loading…</div>';s.emit('openGroup',{id:g.id});if(innerWidth<760)$('peoplePanel').classList.add('closed')}
function renderMessages(arr){$('messages').innerHTML='';if(!arr.length){$('messages').innerHTML='<div class="empty"><div>✦</div><h3>No messages yet</h3><p>Start the conversation.</p></div>';return}arr.forEach(addMessage);scroll()}
function addMessage(m){const d=document.createElement('div');d.className='msg '+(m.from===me.name?'mine':'theirs')+' newMsg';const name=selected.type==='group'&&m.from!==me.name?`<strong class="sender">${escapeHtml(m.from)}</strong>`:'';d.innerHTML=`${name}<div class="bubble">${escapeHtml(m.text)}</div><small>${new Date(m.time).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</small>`;$('messages').append(d);setTimeout(()=>d.classList.remove('newMsg'),500)}
$('composer').onsubmit=e=>{e.preventDefault();const text=$('message').value.trim();if(!text||!selected.type)return;if(selected.type==='direct')s.emit('sendDirect',{to:selected.name,text});else s.emit('sendGroup',{groupId:selected.id,text});$('message').value='';s.emit('stopTyping',{to:selected.name||''})};$('message').oninput=()=>{if(selected.type==='direct'){s.emit('typing',{to:selected.name});clearTimeout(typingTimer);typingTimer=setTimeout(()=>s.emit('stopTyping',{to:selected.name}),700)}};
s.on('directHistory',d=>{if(selected.type==='direct'&&selected.name===d.with)renderMessages(d.messages)});s.on('groupHistory',d=>{if(selected.type==='group'&&selected.id===d.group.id)renderMessages(d.messages)});s.on('directMessage',m=>{if((selected.type==='direct')&&((m.from===me.name&&m.to===selected.name)||(m.to===me.name&&m.from===selected.name))){addMessage(m);scroll()}else if(m.to===me.name||m.from===me.name)highlightPeople(m.from===me.name?m.to:m.from)});s.on('groupMessage',m=>{if(selected.type==='group'&&selected.id===m.groupId){addMessage(m);scroll()}else{const g=groups.find(x=>x.id===m.groupId);if(g)highlightGroup(g)}});s.on('groupCreated',g=>{if(!groups.some(x=>x.id===g.id))groups.unshift(g);renderGroups()});s.on('people',a=>{people=a;renderPeople();if(selected.type==='direct'){const p=a.find(x=>x.name===selected.name);if(p)$('chatStatus').textContent=p.online?'● Online':'Offline'}});s.on('typing',p=>{if(selected.type==='direct'&&p.from===selected.name)$('typing').textContent=p.from+' is typing…'});s.on('stopTyping',p=>{if(p.from===selected.name)$('typing').textContent='' });
function highlightPeople(name){const p=people.find(x=>x.name===name);if(p)renderPeople()}function highlightGroup(g){renderGroups()}function scroll(){$('messages').scrollTop=$('messages').scrollHeight}function escapeHtml(x){return x.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function timeSince(t){if(!t)return'Offline';const m=Math.max(1,Math.floor((Date.now()-t)/60000));return m<60?m+'m ago':Math.floor(m/60)+'h ago'}
$('search').oninput=renderPeople;$('logout').onclick=()=>{s.emit('logout');localStorage.removeItem(SESSION);location.reload()};$('mobilePeople').onclick=()=>$('peoplePanel').classList.remove('closed');$('newGroup').onclick=()=>{buildGroupMembers();$('groupModal').classList.remove('hidden')};$('closeGroup').onclick=()=>$('groupModal').classList.add('hidden');function buildGroupMembers(){$('groupMembers').innerHTML='';people.filter(p=>p.name!==me.name).forEach(p=>{const l=document.createElement('label');l.className='check';l.innerHTML=`<input type="checkbox" value="${escapeHtml(p.name)}"> ${escapeHtml(p.name)}`;$('groupMembers').append(l)})}$('createGroup').onclick=()=>{const name=$('groupName').value.trim();const members=[...$('groupMembers').querySelectorAll('input:checked')].map(x=>x.value);if(!name)return; s.emit('createGroup',{name,members});$('groupName').value='';$('groupModal').classList.add('hidden')};
const old=sessionLoad();if(old?.name){s.emit('login',{name:old.name,accessCode:'112211'})}
