self.addEventListener('push',event=>{let d={title:'FiveChat',body:'New message',kind:'',with:'',groupId:'',url:'/'};try{if(event.data)d=event.data.json()}catch(e){}event.waitUntil(self.registration.showNotification(d.title||'FiveChat',{body:d.body||'New message',tag:'fivechat-'+(d.messageId||Date.now()),renotify:true,data:{kind:d.kind||'',with:d.with||'',groupId:d.groupId||'',url:d.url||'/'},icon:'/favicon.ico',badge:'/favicon.ico'}))});
self.addEventListener('notificationclick',event=>{const data=event.notification.data||{};event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(async list=>{for(const client of list){try{client.postMessage(data);if('focus' in client)return client.focus()}catch(e){}}return clients.openWindow(data.url||'/')}))});
