self.addEventListener('push',event=>{let d={title:'FiveChat',body:'New message',roomName:'',url:'/'};try{if(event.data)d=event.data.json()}catch(e){}event.waitUntil(self.registration.showNotification(d.title||'FiveChat',{body:d.body||'New message',tag:'fivechat-'+(d.roomName||'message'),renotify:true,icon:'/icon.svg',badge:'/icon.svg',data:{url:d.url||'/'}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus' in c){c.navigate(url);return c.focus()}}return clients.openWindow(url)}))});
